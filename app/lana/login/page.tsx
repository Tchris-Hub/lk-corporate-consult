"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, LockKeyhole } from "lucide-react";
export default function Login() { const router = useRouter(); const [error, setError] = useState(""); const [pending, setPending] = useState(false); async function submit(form: FormData) { setPending(true); setError(""); const response = await fetch("/api/lana/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(form)) }); setPending(false); if (!response.ok) return setError((await response.json()).error); router.push("/lana"); router.refresh(); } return <main className="grid min-h-screen place-items-center bg-[#172845] p-5"><form action={submit} className="w-full max-w-md rounded-[2rem] bg-[#fcfaf5] p-8 shadow-2xl"><span className="grid size-12 place-items-center rounded-2xl bg-[#d4af37] text-[#172845]"><LockKeyhole className="size-5" /></span><p className="mt-7 text-xs font-bold tracking-[.2em] text-[#9e7a16]">LK CORPORATE CONSULT</p><h1 className="mt-3 font-serif text-4xl text-[#172845]">Publisher access</h1><p className="mt-3 text-sm leading-6 text-[#5b667a]">Sign in to write, publish and manage regulatory insights.</p><label className="mt-8 block text-sm font-bold">Email<input required name="email" type="email" className="mt-2 w-full rounded-xl border border-[#172845]/15 bg-white px-4 py-3 outline-none focus:border-[#d4af37]" /></label><label className="mt-5 block text-sm font-bold">Password<input required name="password" type="password" className="mt-2 w-full rounded-xl border border-[#172845]/15 bg-white px-4 py-3 outline-none focus:border-[#d4af37]" /></label>{error && <p className="mt-4 text-sm text-red-700">{error}</p>}<button disabled={pending} className="mt-7 flex w-full items-center justify-center gap-2 rounded-full bg-[#d4af37] px-5 py-3.5 font-bold disabled:opacity-60">{pending ? "Signing in…" : "Enter dashboard"}<ArrowRight className="size-4" /></button></form></main>; }
